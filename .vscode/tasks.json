{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build-debug",
            // "command": "cmake -DCMAKE_BUILD_TYPE=Debug .. && cmake --build ${workspaceFolder}/build",
            "command": "./build.sh",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ]
}